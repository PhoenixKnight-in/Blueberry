fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pydantic>=2.6
pydantic-settings>=2.2

# Redis cache in front of the PyPI / GitHub lookups.
redis>=5.0

# Postgres persistence for check history (async driver + ORM + migrations).
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
alembic>=1.13
